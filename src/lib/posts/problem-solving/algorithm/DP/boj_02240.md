---
title: "백준 : 2240. 자두나무"
date: "2025-02-09"
skills: ["Python","Java"]
hashtags: ["dp"]
problem_url: "https://www.acmicpc.net/problem/2240"
description: "3차원 바텀업 DP를 활용한 자두 최대 개수 구하기"
status: "published"
---

# [백준 : 2240. 자두나무](https://www.acmicpc.net/problem/2240)

## **목표**

> T초 동안 자두가 떨어지는데, 최대 W번만 나무를 이동할 수 있을 때 받을 수 있는 자두의 최대 개수를 구하는 문제.
> `바텀업 DP로 state는 현재 위치 n, 현재 나무 위치 l, 이동 횟수 k로 3개로 구분`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().rstrip()

N,K = map(int, input().split())
arr = [0] + [int(input())-1 for _ in range(N)]

dp = [[[0]*(N+1) for _ in range(2)] for _ in range(K+1)]
dp[0][0][1] = int(arr[1] == 0)
dp[1][1][1] = int(arr[1] == 1)

for n in range(1,N):
    for k in range(K+1):
        for l in range(2):
            if k<K:
                dp[k+1][l][n+1] = max(dp[k+1][l][n], dp[k][l^1][n] + int(arr[n+1]==l))
            dp[k][l][n+1] = max(dp[k][l][n+1], dp[k][l][n] + int(arr[n+1]==l))

print(max(dp[k][l][-1] for k in range(K+1) for l in range(2)))
```

[/tabs]

[tabs: Solution 2]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        int N = Integer.parseInt(st.nextToken());
        int K = Integer.parseInt(st.nextToken());
        
        int[] arr = new int[N+1];
        for (int i=1; i<N+1; i++) {
            arr[i] = Integer.parseInt(br.readLine())-1;
        }

        int[][][] dp = new int[K+1][2][N+1];
        dp[0][0][1] = (arr[1]==0 ? 1 : 0);
        dp[1][1][1] = (arr[1]==1 ? 1 : 0);

        for (int n=1; n<N; n++) {
            for (int k=0; k<K+1; k++) {
                for (int l=0; l<2; l++) {
                    if (k<K) {
                        dp[k+1][l][n+1] = Math.max(dp[k+1][l][n], dp[k][l^1][n] + (arr[n+1]==l ? 1 : 0));
                    }
                    dp[k][l][n+1] = Math.max(dp[k][l][n+1], dp[k][l][n] + (arr[n+1]==l ? 1 : 0));
                }
            }
        }
        
        int answer = 0;
        for (int k=0; k<K+1; k++) {
            for (int l=0; l<2; l++) {
                answer = Math.max(answer, dp[k][l][N]);
            }
        }
        System.out.println(answer);
    }
}
```

[/tabs]

* SOLUTION 1
  * 바텀업 DP: state는 3개로 구분 - 현재 위치 n, 현재 나무 위치 l, 이동 횟수 k. dp[k][l][n]으로 3차원 DP 테이블 작성
  * 전이: n에서 n+1로 넘어갈 때 l을 바꿀지 안바꿀지에 따라서 k값 증가유무가 결정됨. 이동하지 않으면 l도 고정이고, 이동하면 k가 증가하면서 l은 토글됨
  * 기본 로직: 다음 노드에 도착하면 사과 유무로 + 1 해주기
* SOLUTION 2
  * Java 구현: 동일한 3차원 DP 로직을 Java로 구현

## **코멘트**

* .
