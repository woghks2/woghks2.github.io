---
title: "백준 : 2011. 암호코드"
date: "2024-12-31"
skills: ["Python"]
hashtags: ["dp"]
problem_url: "https://www.acmicpc.net/problem/2011"
description: "탑다운 DP를 활용한 암호코드 해석 방법의 수 구하기"
status: "published"
---

# [백준 : 2011. 암호코드](https://www.acmicpc.net/problem/2011)

## **목표**

> 숫자로 이루어진 암호를 알파벳으로 변환하는 방법의 수를 구하는 문제.
> `탑다운 DP로 바텀업보다는 탑다운으로 조건에 맞는 경우에만 탐색 진행`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
sys.setrecursionlimit(10**6)

string = input()
L = len(string)

dp = [-1]*L
MOD = 1000000

def dfs(x):
    
    if x == L:
        return 1
    
    if dp[x] != -1:
        return dp[x]
    
    result = 0
    
    if string[x] != '0':
        result += dfs(x+1)
    
    if x+1 < len(string) and '10' <= string[x:x+2] <= '26':
        result += dfs(x+2)
    
    dp[x] = result%MOD
    
    return dp[x]

print(dfs(0))
```

[/tabs]

* SOLUTION 1
  * 바텀업/탑다운: 바텀업으로 state 나눠서 전 노드/전전 노드에서 들어오는 방식으로 풀기. 탑다운으로 조건에 맞는 경우에만 탐색 진행하기
  * DP 구조: 탑다운 - 종료조건 + 메모제이션 + 탐색조건 + DP 업데이트

## **코멘트**

* 탑다운 : 종료조건 + 메모제이션 + 탐색조건 + DP 업데이트
