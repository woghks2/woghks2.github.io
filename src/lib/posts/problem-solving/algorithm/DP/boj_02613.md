---
title: "백준 : 2613. 숫자구슬"
date: "2025-04-10"
skills: ["Python"]
hashtags: ["dp"]
problem_url: "https://www.acmicpc.net/problem/2613"
description: "2차원 DP + 역추적을 활용한 숫자구슬 최소 그룹 합 최댓값 구하기"
status: "published"
---

# [백준 : 2613. 숫자구슬](https://www.acmicpc.net/problem/2613)

## **목표**

> N개의 숫자구슬을 M개의 그룹으로 나눌 때, 각 그룹의 합 중 최댓값을 최소화하는 문제.
> `2차원으로 줄여서 풀이. group을 g개로 분할했을 때, 인덱스 x까지 탐색했을때 그룹의 최대값을 dp에 저장하고 역추적`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
N,M = map(int,input().split())
arr = list(map(int,input().split()))

INF = float('inf')

cumsum = [0]
for a in arr:
    cumsum.append(cumsum[-1]+a)

dp = [[INF]*(N+1) for _ in range(M+1)]
dp[0][0] = 0
trace = [[-1]*(N+1) for _ in range(M+1)]

def dfs(g,x):

    if not g:
        if x:
            return INF # 불가능
        return 0 # 기저조건

    # 메모제이션
    if dp[g][x] != INF:
        return dp[g][x]
    
    for s in range(g-1,x):
        cost = max(dfs(g-1,s), cumsum[x] - cumsum[s])
        if cost < dp[g][x]:
            dp[g][x] = cost
            trace[g][x] = s
    
    return dp[g][x]

print(dfs(M,N))

path = []
x = N
for i in range(M,0,-1):
    k = trace[i][x]
    path.append(x-k)
    x = k

print(*path[::-1])
```

[/tabs]

* SOLUTION 1
  * 2차원 DP: 현재 배열에서 분할 횟수가 remain번 남았을 때, [s,e] 구간을 [s,m] [s,m+1]로 분할. 배열 크기가 N^3이라 2700만이므로 2차원으로 줄여서 풀이
  * 역추적: LCS에서 역추적하는 것 처럼 추적. 최소값을 갱신할 때, 이전 인덱스 변화를 저장

## **코멘트**

* 어렵
