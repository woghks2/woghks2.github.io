---
title: "백준 : 1888. 곰팡이"
date: "2024-01-17"
skills: ["Python"]
hashtags: ["simulation"]
problem_url: "https://www.acmicpc.net/problem/1888"
description: "BFS + 시뮬레이션을 활용한 곰팡이 덩어리 통합 문제"
status: "published"
---

# [백준 : 1888. 곰팡이](https://www.acmicpc.net/problem/1888)

## **목표**

> 곰팡이가 사방으로 퍼져나가면서 여러 덩어리가 하나로 통합되는 최소 일수를 구하는 문제.
> `곰팡이가 사방으로 퍼져나가고, 한 덩어리로 된 것을 판별하려면 각 군집별로 disjoint set을 사용하거나 BFS를 돌려서 개수를 카운트. 곰팡이 군집을 만날때마다 카운팅을 해줘서 1개가 되는 순간의 날짜를 출력`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
from collections import deque
import sys
input = lambda : sys.stdin.readline().rstrip()

h,w = map(int,input().split())
arr = [list(map(int,input())) for _ in range(h)]
dire = [(1,0),(0,1),(-1,0),(0,-1)]
inside = lambda x,y : 0<=x<w and 0<=y<h

def bfs(x,y,n): # 곰팡이 덩어리 BFS
    visit[y][x] = True
    q = deque([(x,y)])
    while q:
        x,y = q.popleft()
        for dx,dy in dire:
            nx,ny = x+dx,y+dy
            if inside(nx,ny) and not visit[ny][nx] and arr[ny][nx]:
                visit[ny][nx] = True
                q.append((nx,ny))

def check(): # 곰팡이 덩어리 개수 세는 함수
    cnt = 0
    for i in range(h):
        for j in range(w):
            if arr[i][j] and not visit[i][j]:
                bfs(j,i,arr[i][j])
                cnt += 1
    return cnt      

def spread():
    narr = [[0]*w for _ in range(h)]
    for y in range(h):
        for x in range(w):
            speed = arr[y][x]
            narr[y][x] = max(narr[y][x],arr[y][x])
            if speed:
                for nx in range(x-speed,x+speed+1):
                    for ny in range(y-speed,y+speed+1):
                        if inside(nx,ny) and arr[ny][nx] < arr[y][x]:
                            narr[ny][nx] = max(narr[ny][nx],arr[y][x])
    return narr
                
day = 0
visit = [[False]*w for _ in range(h)]
count = check()
while count > 1:
    visit = [[False]*w for _ in range(h)]
    arr = spread()
    count = check()
    day += 1
print(day)
```

[/tabs]

* SOLUTION 1
  * 곰팡이 퍼짐: 곰팡이가 사방으로 퍼져나감
  * 덩어리 판별: 한 덩어리로 된 것을 판별하려면 각 군집별로 disjoint set을 사용하거나 BFS를 돌려서 개수를 카운트
  * BFS 카운팅: 곰팡이 군집을 만날때마다 카운팅을 해줘서 1개가 되는 순간의 날짜를 출력

## **코멘트**

* 
