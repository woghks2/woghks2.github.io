---
title: "백준 : 1083. 소트"
date: "2024-11-14"
skills: ["Python"]
hashtags: ["greedy"]
problem_url: "https://www.acmicpc.net/problem/1083"
description: "백준 : 1083. 소트 문제 풀이"
status: "published"
---

# [백준 : 1083. 소트](https://www.acmicpc.net/problem/1083)

## **목표**

> 주어진 배열에서 S번 이내의 swap으로 만들 수 있는 사전순으로 가장 뒤에 있는 배열을 구하는 문제.
> `각 자리수 마다 남은 횟수 S에 대해서 Swap 가능한 범위가 주어짐. Swap 범위 내에서 해당 자리보다 큰 값이 주어지면 Swap을 반복`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
N = int(input())
arr = list(map(int,input().split()))
T = int(input())

# 역 사전순으로 가장 앞서는 경우
goal = sorted(arr,reverse=True)

# 해당 인덱스 a에서 앞으로 한칸 swap
def swap(array: list, a: int)->list:
    array[a],array[a-1] = array[a-1],array[a]
    return array

while T:
    
    # 처음부터 스왑이 불가능한 경우 break로 루프를 탈출한다.
    if arr == goal:
        break
    
    for i in range(N-1):
        temp_max = max(arr[i+1:i+1+T])
        if arr[i] < max(arr[i+1:i+1+T]):
            arr = swap(arr,arr.index(temp_max))
            T -= 1
            break
print(*arr)
```

[/tabs]

* SOLUTION 1
  * Swap 범위: 각 자리수 마다 남은 횟수 S에 대해서 Swap 가능한 범위가 주어짐
  * Swap 반복: Swap 범위 내에서 해당 자리보다 큰 값이 주어지면 Swap을 반복
  * 시간 복잡도: $N=50$이라 swap만 제대로 구현하면 시간적으로 문제될 일은 없음

## **코멘트**

* 출력형식 잘 보기.
