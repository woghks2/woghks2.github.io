---
title: "백준 : 14391. 종이 조각"
date: "2023-10-22"
skills: ["Python"]
hashtags: ["bruteforce"]
problem_url: "https://www.acmicpc.net/problem/14391"
description: "비트마스킹 + BFS를 활용한 종이 조각 최대값 구하기"
status: "published"
---

# [백준 : 14391. 종이 조각](https://www.acmicpc.net/problem/14391)

## **목표**

> 종이를 가로 또는 세로로 잘라서 만든 숫자들의 합의 최댓값을 구하는 문제.
> `비트마스킹으로 각 칸의 방향(가로/세로)을 정하고, BFS로 연결된 숫자 합 계산`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
from itertools import product as PI
from collections import deque
import sys
input = lambda: sys.stdin.readline().strip()

# product로 mask 만들기
h,w = map(int,input().split())
arr = ''
for _ in range(h):
    arr += input()
masks = list(PI([0,1],repeat=h*w))

# BFS로 어떤 방향으로 읽을지 정하기.
def bfs(x,num):
    
    # 문자열 s에 숫자를 붙여나간다.
    s = arr[x]
    q = deque([x])
    visit[x] = True
    
    while q:
        x = q.popleft()
        nx = x+1 if not num else x+w
        if not num: # 가로방향으로 읽기
            nx = x+1
            if nx%w and not visit[nx] and mask[nx] == num:
                s += arr[nx]
                visit[nx] = True
                q.append(nx)
        else: # 세로방향으로 읽기
            nx = x+w
            if 0<=nx<w*h and not visit[nx] and mask[nx] == num:
                s += arr[nx]
                visit[nx] = True
                q.append(nx)
    return int(s)

# 각 칸이 방문 안됐으면 BFS로
answer = 0
for mask in masks:
    visit = [False]*w*h
    score = 0
    # 방문하지 않은 곳이면 BFS돌리고 값 더해주기
    for i in range(h*w):
        if not visit[i]:
            score += bfs(i,mask[i])
    answer = max(answer,score)
print(answer)
```

[/tabs]

* SOLUTION 1
  * 비트마스킹: 맵 사이즈 최대 16칸이므로 각 칸마다 2가지 방향(가로/세로)을 비트로 표현
  * itertools.product: 모든 경우의 수를 생성하여 각 칸의 방향 조합 탐색
  * BFS: 같은 방향으로 연결된 칸들을 그룹화하여 숫자로 변환

## **코멘트**

* 이동방향이 오른쪽, 아래쪽 두 가지라서 while문 쓰는게 더 빨랐을 듯.
