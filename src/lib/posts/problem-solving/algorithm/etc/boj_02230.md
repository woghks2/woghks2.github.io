---
title: "백준 : 2230. 수 고르기"
date: "2024-10-02"
skills: ["Python"]
hashtags: ["etc"]
problem_url: "https://www.acmicpc.net/problem/2230"
description: "투포인터를 활용한 수 고르기 문제"
status: "published"
---

# [백준 : 2230. 수 고르기](https://www.acmicpc.net/problem/2230)

## **목표**

> 주어진 수열에서 두 수의 차이가 M 이상이 되는 가장 작은 차이를 구하는 문제.
> `입력이 10만 이상이라 $O(N^2)$로는 불가능. 투 포인터로 제한적인 영역을 탐색하면서 $O(N)$으로 풀어주기`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().rstrip()

N,M = map(int,input().split())

arr = [int(input()) for _ in range(N)]
arr.sort()

l,r = 0,0
answer = float('inf')

while l<=r<N:
    d = arr[r] - arr[l]
    if M <= d:
        answer = min(answer,d)
        l += 1
    else:
        r += 1

print(answer)
```

[/tabs]

* SOLUTION 1
  * 시간 복잡도: 입력이 10만 이상이라 $O(N^2)$로는 불가능
  * 투포인터: 투 포인터로 제한적인 영역을 탐색하면서 $O(N)$으로 풀어주기

## **코멘트**

* while 조건에서, l==r까지는 가능하다.
* 같은 숫자에서 조건을 만족시키고, l+=1로 탈출하면 된다.
* 양 쪽에서 포인터를 조이는지, 같은 지점에서 출발하는지는 더 적은 영역을 탐색하는 것을 생각하면 자연스럽게 알 수 있다.
