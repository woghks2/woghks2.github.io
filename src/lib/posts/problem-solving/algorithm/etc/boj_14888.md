---
title: "백준 : 14888. 연산자 끼워넣기"
date: "2025-04-05"
skills: ["Python"]
hashtags: ["etc"]
problem_url: "https://www.acmicpc.net/problem/14888"
description: "백트래킹을 활용한 연산자 끼워넣기 문제"
status: "published"
---

# [백준 : 14888. 연산자 끼워넣기](https://www.acmicpc.net/problem/14888)

## **목표**

> 주어진 수열에 연산자를 끼워넣어 만들 수 있는 식의 결과값 중 최댓값과 최솟값을 구하는 문제.
> `dfs 완탐`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
N = int(input())
arr = list(map(int,input().split()))
ops = list(map(int,input().split()))

answer = [-float('inf'),float('inf')]
def dfs(cnt,val):

    if cnt == N:
        answer[0] = max(answer[0],val)
        answer[1] = min(answer[1],val)

    for idx,op in enumerate(ops):
        if op:
            ops[idx] -= 1
            if idx == 0:
                dfs(cnt+1,val+arr[cnt])
            elif idx == 1:
                dfs(cnt+1,val-arr[cnt])
            elif idx == 2:
                dfs(cnt+1,val*arr[cnt])
            else:
                if val < 0:
                    dfs(cnt+1,-((-val)//arr[cnt]))
                else:
                    dfs(cnt+1,val//arr[cnt])
            ops[idx] += 1
dfs(1,arr[0])
for ans in answer:
    print(ans)
```

[/tabs]

* SOLUTION 1
  * 완전 탐색: dfs 완탐

## **코멘트**

* 음수 조건 처리해주기
