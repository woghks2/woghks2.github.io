---
title: "SWEA : 2117. 홈 방범 서비스"
date: "2025-04-05"
skills: ["Java"]
hashtags: ["etc"]
problem_url: "https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AV5V61LqAf8DFAWu"
description: "완전탐색을 활용한 홈 방범 서비스 문제"
status: "published"
---

# [SWEA : 2117. 홈 방범 서비스](https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AV5V61LqAf8DFAWu)

## **목표**

> 마름모 모양의 서비스 영역에서 손해를 보지 않으면서 서비스할 수 있는 최대 집의 개수를 구하는 문제.
> `완탐. 마름모 모양은 맨해튼 거리의 abs에 제한되는 경우임`

## **문제 풀이**

### **Java**

[tabs: Solution 1]

```java
import java.io.*;
import java.util.*;
 
public class Solution {
 
    static int N,M;
    static int[][] arr;
 
    public static boolean inside(int x, int y) {
        return 0 <= x && x < N && 0 <= y && y < N;
    }
 
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
 
        int T = Integer.parseInt(br.readLine());
        for (int tc = 1; tc <= T; tc++) {
 
            // read input
            st = new StringTokenizer(br.readLine());
            N = Integer.parseInt(st.nextToken());
            M = Integer.parseInt(st.nextToken());
 
            arr = new int[N][N];
            for (int y = 0; y < N; y++) {
                st = new StringTokenizer(br.readLine());
                for (int x = 0; x < N; x++) {
                    arr[y][x] = Integer.parseInt(st.nextToken());
                }
            }
 
            int answer = 1;
            for (int y = 0; y < N; y++) {
                for (int x = 0; x < N; x++) {
                    for (int k = 1; k <= 2*N+1; k++) {
                        int cnt = 0;
                        for (int nx = x - k + 1; nx <= x + k - 1; nx++) {
                            for (int ny = y - k + 1; ny <= y + k - 1; ny++) {
                                if (inside(nx, ny) && Math.abs(x - nx) + Math.abs(y - ny) < k) {
                                    if (arr[ny][nx] == 1) {
                                        cnt++;
                                    }
                                }
                            }
                        }
                        int cost = k * k + (k - 1) * (k - 1);
                        if (cnt * M >= cost) {
                            answer = Math.max(answer, cnt);
                        }
                    }
                }
            }
             
            System.out.println("#" + tc + " " + answer);
        }
    }
}
```

[/tabs]

* SOLUTION 1
  * 완전탐색: 완탐
  * 마름모: 마름모 모양은 맨해튼 거리의 abs에 제한되는 경우임

## **코멘트**

* 
