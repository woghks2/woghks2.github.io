---
title: "백준 : 1300. K번째 수"
date: "2025-04-21"
skills: ["Python"]
hashtags: ["etc"]
problem_url: "https://www.acmicpc.net/problem/1300"
description: "이분탐색을 활용한 K번째 수 문제"
status: "published"
---

# [백준 : 1300. K번째 수](https://www.acmicpc.net/problem/1300)

## **목표**

> N×N 크기의 배열에서 각 원소를 오름차순으로 정렬했을 때 K번째 수를 구하는 문제.
> `이분탐색. 각 row가 정렬되있음을 바로 확인할 수 있음. 각 row에서는 이분탐색으로 진행할 수 있고, 등차수열이라 나눗셈으로 바로 몫을 구할 수 있음`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
N = int(input())
K = int(input())

min_val,max_val = 0,N**2+1

while min_val+1 < max_val:
    val = (min_val+max_val)//2

    cnt = 0
    for y in range(1,N+1):
        cnt += min(N,val//y)

    if cnt < K:
        min_val = val
    else:
        max_val = val

print(max_val)
```

[/tabs]

* SOLUTION 1
  * 이분탐색: 이분탐색
  * 규칙 활용: 각 row가 정렬되있음을 바로 확인할 수 있음. 각 row에서는 이분탐색으로 진행할 수 있고, 등차수열이라 나눗셈으로 바로 몫을 구할 수 있음
  * 범위 설정: min value, max value는 구간에 포함되지 않도록 잡아주고, while문 조건을 min_val+1 < max_val로 잡음

## **코멘트**

* 규칙성이 있는 array에서는 굳이 이분탐색을 사용하지 않아도 된다는 점 생각하기.
