---
title: "백준 : 6497. 전력난"
date: "2024-04-13"
skills: ["Python"]
hashtags: ["graph"]
problem_url: "https://www.acmicpc.net/problem/6497"
description: "MST를 활용한 전력난 문제"
status: "published"
---

# [백준 : 6497. 전력난](https://www.acmicpc.net/problem/6497)

## **목표**

> 모든 집에 전력을 공급하면서 절약할 수 있는 최대 비용을 구하는 문제.
> `MST를 만들어서 필요없는 간선의 cost가 얼마인지 구하자`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().rstrip()

def find(a):
    if parent[a] != a:
        parent[a] = find(parent[a])
    return parent[a]

def union(a,b):
    parent[max(a,b)] = min(a,b)

while True:

    # TEST CASE : 입력 -> 간선
    N,K = map(int,input().split())
    if (N,K) == (0,0):
        break

    # edges를 내림차순으로 정렬
    edges = [tuple(map(int,input().split())) for _ in range(K)]
    edges.sort(key = lambda x:-x[-1])
    parent = [i for i in range(N)]

    # 연결하지 않는 간선 / 연결한 간선 코스트 세주기.
    total,cnt = 0,0
    while edges:
        a,b,cost = edges.pop()
        pa,pb = find(a),find(b)
        total += cost
        if pa != pb:
            union(pa,pb)
            cnt += cost
    print(total-cnt)
```

[/tabs]

* SOLUTION 1
  * MST: MST를 만들어서 필요없는 간선의 cost가 얼마인지 구하자

## **코멘트**

* 기본 MST 문제
