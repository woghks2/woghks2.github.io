---
title: "백준 : 16681. 등산"
date: "2025-02-03"
skills: ["Python", "Java"]
hashtags: ["graph"]
problem_url: "https://www.acmicpc.net/problem/16681"
description: "다익스트라를 활용한 등산 문제"
status: "published"
---

# [백준 : 16681. 등산](https://www.acmicpc.net/problem/16681)

## **목표**

> 등산의 가치(성취감 - 소모한 체력)를 최대로 하는 문제.
> `성취감은 높이로 이미 고정되어있음. 등산의 가치를 최대로 하려면 소모한 체력을 최소로 해야함. 즉, 이동 거리가 최소여야하는 최단 거리 문제에 노드 간 이동 조건이 붙은 문제`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import heapq as hq
import sys

input = lambda : sys.stdin.readline().rstrip()
INF = float('inf')

N,M,D,E = map(int,input().split())
heights = [None] + list(map(int,input().split()))

G = [[] for _ in range(N+1)]
for _ in range(M):
    a,b,cost = map(int,input().split())
    G[a].append((b,cost))
    G[b].append((a,cost))

def di(sx):
    heap = [(0,sx)]
    V = [INF]*(N+1)
    V[sx] = 0

    while heap:
        
        t,x = hq.heappop(heap)
        
        if t > V[x]:
            continue

        for nx,cost in G[x]:
            nt = t+cost
            if heights[x] < heights[nx] and t < V[nx]:
                hq.heappush(heap,(nt,nx))
                V[nx] = nt
    return V

up, down = di(1), di(N)
values = []
for i in range(1,N+1):
    if up[i] == INF or down[i] == INF:
        continue
    values.append(heights[i]*E-(up[i]+down[i])*D)
print(max(values) if values else "Impossible")
```

[/tabs]

* SOLUTION 1
  * 다익스트라: 등산의 가치를 최대로 하려면 소모한 체력을 최소로 해야함
  * 접근 방법: 1 -> N -> 1로 이동을 해야하므로 다익 2번 돌리기. 1 -> N으로 이동이 가능하다는거 자체가 N -> 1도 가능하다는 소리

## **코멘트**

* 제네릭이라서 이중 List 구현 시, static에서 선언 해놓고 내부 리스트 생성하면서 각 인덱스에 리스트를 한 번 더 초기화한다.
* 파이썬을 풀 땐 오버플로우 생각을 안해서 몰랐는데, 간선 사이즈도 보면서 자료형까지 생각하는 것 잊지 않기.
