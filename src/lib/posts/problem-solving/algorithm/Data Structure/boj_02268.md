---
title: "백준 : 2268. 수들의 합 7"
date: "2024-01-01"
skills: ["Python"]
hashtags: ["datastructure"]
problem_url: "https://www.acmicpc.net/problem/2268"
description: "펜윅 트리를 활용한 구간 합 및 업데이트"
status: "published"
---

# [백준 : 2268. 수들의 합 7](https://www.acmicpc.net/problem/2268)

## **목표**

> 배열의 구간 [a, b] 합을 구하고, 특정 인덱스의 값을 변경하는 문제.
> `펜윅 트리로 구간합을 효율적으로 구하기`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().strip()

N,M = map(int,input().split())

arr = [0]*(N+1)
tree = [0]*(N+1)

def update(x,diff):
    i = x
    while i<=N:
        tree[i] += diff
        i += i&(-i)

def cumsum(x):
    result = 0
    i = x
    while i:
        result += tree[i]
        i -= i&(-i)
    return result

for _ in range(M):
    cmd,a,b = map(int,input().split())
    if cmd:
        diff = b - arr[a]
        arr[a] = b
        update(a,diff)
    else:
        print(cumsum(max(a,b)) - cumsum(min(a,b)-1))
```

[/tabs]

* SOLUTION 1
  * 펜윅 트리: 구간합 구하는데는 펜윅트리가 더 빠르다고 해서 펜윅트리로 풀이
  * 업데이트: 상위 노드(?)으로 전파해주기
  * 누적합: 누적합 같은 경우에는 0쪽으로 이동해주기

## **코멘트**

* 홀수들은 자기 자신 정보만
* 짝수들은 특정 구간합을 가지고 있다.
* [Reference](https://www.youtube.com/watch?v=fg2iGP4e2mc&ab_channel=%EB%8F%99%EB%B9%88%EB%82%98)
* 업데이트는 상위 노드(?)으로 전파해주기
* 누적합 같은 경우에는 0쪽으로 이동해주기
