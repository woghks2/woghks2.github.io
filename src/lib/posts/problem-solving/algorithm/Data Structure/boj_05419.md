---
title: "백준 : 5419. 북서풍"
date: "2024-01-01"
skills: ["Python"]
hashtags: ["datastructure"]
problem_url: "https://www.acmicpc.net/problem/5419"
description: "좌표 압축 + 펜윅 트리를 활용한 역순 쌍 개수 계산"
status: "published"
---

# [백준 : 5419. 북서풍](https://www.acmicpc.net/problem/5419)

## **목표**

> 북서쪽에서 남동쪽으로 불어오는 바람으로 연결되는 섬의 쌍의 개수를 구하는 문제.
> `좌표 압축 + 펜윅 트리로 값이 매우 크고 상대적인 대소 관계만 중요한 경우 처리`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().strip()

def update(x,diff):
    i = x
    while i<=size:
        fenwick[i] += diff
        i += i&(-i)

def query(x):
    i = x
    result = 0
    while i:
        result += fenwick[i]
        i -= i&(-i)
    return result

TC = int(input())
for _ in range(TC):
    N = int(input())
    arr = sorted([tuple(map(int,input().split())) for _ in range(N)],
                key = lambda x:(x[0],-x[1]))
    
    y_coord = sorted(set(y for _,y in arr), reverse=True)
    cvt_comp = {val:idx+1 for idx,val in enumerate(y_coord)}
    
    size = len(cvt_comp)
    fenwick = [0]*(size+1)
    
    answer = 0
    for x,y in arr:
        y_idx = cvt_comp[y]
        cnt = query(y_idx)
        answer += cnt
        update(y_idx,1)
    
    print(answer)
```

[/tabs]

* SOLUTION 1
  * 좌표 압축: 값이 매우 크고 상대적인 대소 관계만 중요하므로, 좌표 압축 후 펜윅 트리 사용
  * MLE 방지: 10억에 가까운 값들을 트리에 모두 저장하면 MLE 발생하므로 좌표 압축 필요
  * 정렬: x좌표 기준 오름차순, y좌표 기준 내림차순 정렬 후 처리

## **코멘트**

* 어렵다...
