---
title: "백준 : 14438. 수열과 쿼리 17"
date: "2024-01-01"
skills: ["Python"]
hashtags: ["datastructure"]
problem_url: "https://www.acmicpc.net/problem/14438"
description: "세그먼트 트리를 활용한 구간 최솟값 구하기"
status: "published"
---

# [백준 : 14438. 수열과 쿼리 17](https://www.acmicpc.net/problem/14438)

## **목표**

> 배열의 특정 인덱스 값을 변경하고, 구간 [b, c]에서 최솟값을 구하는 문제.
> `세그먼트 트리로 구간 최솟값을 효율적으로 구하기`

## **문제 풀이**

### **Python**

[tabs: Solution 1]

```python
import sys
input = lambda : sys.stdin.readline().strip()

N = int(input())
arr = list(map(int,input().split()))
segtree = [float('inf')]*(4*N)

def build(x,s,e):
    if s==e:
        segtree[x] = min(segtree[x],arr[s])
        return segtree[x]
    m = (s+e)//2
    lv = build(2*x+1,s,m)
    rv = build(2*x+2,m+1,e)
    segtree[x] = min(segtree[x], lv, rv)
    return segtree[x]

def update(x,s,e,idx,val):
    if s==e:
        arr[idx] = val
        segtree[x] = arr[idx]
        return segtree[x]
    m = (s+e)//2
    if idx<=m:
        lv = update(2*x+1,s,m,idx,val)
        rv = segtree[2*x+2]
    else:
        lv = segtree[2*x+1]
        rv = update(2*x+2,m+1,e,idx,val)
    segtree[x] = min(lv,rv)
    return segtree[x]

def query(x,s,e,l,r):
    if l<=s and e<=r:
        return segtree[x]
    if e<l or s>r:
        return float('inf')
    m = (s+e)//2
    lv = query(2*x+1,s,m,l,r)
    rv = query(2*x+2,m+1,e,l,r)
    return min(lv,rv)

build(0,0,N-1)
for _ in range(int(input())):
    a,b,c = map(int,input().split())
    if a==1:
        update(0,0,N-1,b-1,c)
    else:
        print(query(0,0,N-1,b-1,c-1))
```

[/tabs]

* SOLUTION 1
  * 세그먼트 트리: 최솟값, 최댓값, 덧셈, 곱셈 등 기본 반환값만 지켜주면 됨
  * 0-based 인덱싱: 기본적으로 세그트리 구현은 0-based로 구현하고, 쿼리만 처리

## **코멘트**

* 기본적으로 세그트리 구현은 0-based로 구현하고, 쿼리만 처리.
