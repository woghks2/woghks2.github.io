---
title: "leetcode : 602. Friend Requests II: Who Has the Most Friends"
date: "2025-01-07"
skills: ["SQL", "Pandas"]
hashtags: ["leetcode"]
problem_url: "https://leetcode.com/problems/friend-requests-ii-who-has-the-most-friends/description/"
description: "UNION"
status: "published"
---

# [leetcode : 602. Friend Requests II: Who Has the Most Friends](https://leetcode.com/problems/friend-requests-ii-who-has-the-most-friends/description/)

## **다이어그램**

```mermaid
erDiagram
    REQUEST_ACCEPTED {
        int requester_id PK
        int accepter_id PK
        date accept_date
    }

    REQUEST_ACCEPTED ||--|| REQUEST_ACCEPTED : "PK(requester_id, accepter_id)"
```

## **목표**

> Write a solution to `find the people who have the most friends and the most friends number.`
> The test cases are generated so that only one person has the most friends.
> `가장 많은 친구를 가진 사람과 친구 수 구하기`

## **문제 풀이**

### **MySQL**

[tabs: Solution 1, Solution 2, Solution 3]

```sql
-- SOLUTION 1: CTE + UNION ALL
WITH REQS AS (
    SELECT REQUESTER_ID, COUNT(*) AS NUM
    FROM REQUESTACCEPTED
    GROUP BY REQUESTER_ID
),
ACEPS AS (
    SELECT ACCEPTER_ID, COUNT(*) AS NUM
    FROM REQUESTACCEPTED
    GROUP BY ACCEPTER_ID
),
TEMP AS (
    SELECT REQUESTER_ID AS ID, NUM
    FROM REQS
    UNION ALL
    SELECT ACCEPTER_ID AS ID, NUM
    FROM ACEPS
)
SELECT ID, SUM(NUM) AS NUM
FROM TEMP
GROUP BY ID
ORDER BY NUM DESC
LIMIT 1
```

```sql
-- SOLUTION 2: 서브쿼리 + UNION ALL
SELECT
    ID,
    SUM(NUM) AS NUM
FROM (
    SELECT
        REQUESTER_ID AS ID,
        COUNT(*) AS NUM
    FROM
        REQUESTACCEPTED
    GROUP BY
        REQUESTER_ID
    UNION ALL
    SELECT
        ACCEPTER_ID AS ID,
        COUNT(*) AS NUM
    FROM
        REQUESTACCEPTED
    GROUP BY
        ACCEPTER_ID
    ) AS T
GROUP BY
    ID
ORDER BY
    SUM(NUM) DESC
LIMIT 1
```

```sql
SELECT
    ID,
    COUNT(*) AS NUM
FROM (
    SELECT
        REQUESTER_ID AS ID
    FROM
        REQUESTACCEPTED
    UNION ALL
    SELECT
        ACCEPTER_ID AS ID
    FROM
        REQUESTACCEPTED
    ) AS T
GROUP BY
    ID
ORDER BY
    COUNT(*) DESC
LIMIT 1
```

[/tabs]

* SOLUTION 1
  * CTE를 통해서 각 사람이 몇 번 요청을 보내고, 몇 번 받았는지 확인하기.
  * UNION을 통해서 유저별 받은 수, 보낸 수를 GROUP BY로 통합해서 계산하기.

* SOLUTION 2
  * req에서 한 번 GROUP BY + COUNT, acpt에서 한 번 GROUP BY + COUNT
  * UNION ALL로 합쳐서 계산해주기.

* SOLUTION 3
  * 먼저 id로 바꾼다음에 합친 후에 group by를 진행해서 연산 수를 줄인다.

### **Pandas**

[tabs: Solution 1, Solution 2, Solution 3, Solution 4]

```python
# SOLUTION 1: iterrows
import pandas as pd
from collections import defaultdict

def most_friends(request_accepted: pd.DataFrame) -> pd.DataFrame:
    table = defaultdict(int)
    for _, row in request_accepted.iterrows():
        a, b = row['requester_id'], row['accepter_id']
        table[a] += 1
        table[b] += 1
    
    iden, num = max(table.items(), key=lambda x: x[1])
    return pd.DataFrame({'id': [iden], 'num': [num]})
```

```python
# SOLUTION 2: groupby + concat
import pandas as pd

def most_friends(request_accepted: pd.DataFrame) -> pd.DataFrame:
    group1 = request_accepted.groupby('requester_id').size().reset_index(name='num').rename(columns={'requester_id': 'id'})
    group2 = request_accepted.groupby('accepter_id').size().reset_index(name='num').rename(columns={'accepter_id': 'id'})
    answer = pd.concat([group1, group2])
    answer_grouped = answer.groupby('id').sum().reset_index()
    return answer_grouped.loc[answer_grouped['num']==answer_grouped['num'].max(), ['id','num']]
```

```python
# SOLUTION 3: value_counts + concat
import pandas as pd

def most_friends(request_accepted: pd.DataFrame) -> pd.DataFrame:
    cnt1 = pd.DataFrame(request_accepted['requester_id'].value_counts().reset_index(name='num')).rename(columns={'requester_id':'id'})
    cnt2 = pd.DataFrame(request_accepted['accepter_id'].value_counts().reset_index(name='num')).rename(columns={'accepter_id':'id'})
    answer = pd.concat([cnt1,cnt2]).groupby('id').sum().reset_index()
    return answer.loc[answer['num']==answer['num'].max(), ['id','num']]
```

```python
# SOLUTION 4: concat + value_counts
import pandas as pd

def most_friends(request_accepted: pd.DataFrame) -> pd.DataFrame:
    return (
        pd.concat([request_accepted.requester_id, request_accepted.accepter_id])
        .to_frame('id')
        .value_counts()
        .reset_index(name='num')
        .head(1)
    )
```

[/tabs]

* SOLUTION 1
  * 한 번의 순회로 카운팅을 해주는게 나을거같아서 iterrows로 풀이.
  * 다른 추가 연산이 없어서 속도도 다른 코드들에 비해 나쁘지 않은편

* SOLUTION 2
  * groupby + concat으로 요청 수 합해주기.
  * 마지막에는 max로 조회

* SOLUTION 3
  * value_counts + concat으로 풀이

* SOLUTION 4
  * Solution 2,3을 더 간결하게 풀 수 있다.

## **코멘트**

* 기본 문제
